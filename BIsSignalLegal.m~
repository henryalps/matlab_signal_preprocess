function isLegal = BIsSignalLegal(sig)
    THEROLD = 0.7;
    %% 先把所有的NaN替换掉
    
    %% 求一阶差分零值数
    d1 = sum(size(diff(sig)~=0)) ;
    %% 若一阶差分始终为0，说明始终是一个值
    if == 0
        isLegal = FALSE;
        return
    end
    %% 若一阶差分的0值率超过阈值，说明大部分数据为无效数据
    if 
end