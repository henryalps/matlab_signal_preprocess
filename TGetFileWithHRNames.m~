% clear all
% load 'matNames.mat'
% newMatNames = matNames;
% for i=1:length(matNames)
%     tmp = load(matNames{i});
%     if ~isfield(tmp,'rpos')
%         newMatNames{i}='';
%     end
% end
% legalPos = newMatNames~='';
% newMatNames = newMatNames{legalPos};
% 
% save('newMatNames.mat','newMatNames')

leagalPos = ~cellfun(@isempty, newMatNames);
